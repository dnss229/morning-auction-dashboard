# 早盘集合竞价表现

一个适合手机查看的早盘集合竞价静态看板。

## Vercel 发布

1. 把本目录上传到 GitHub 仓库。
2. 在 Vercel 选择 `Add New` -> `Project`。
3. 导入这个 GitHub 仓库。
4. Framework Preset 选择 `Other`，Build Command 和 Output Directory 留空。
5. 点击 `Deploy`。

发布后，手机打开 Vercel 网址，可以通过浏览器菜单添加到主屏幕。
